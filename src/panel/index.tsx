/**
 * Module Federation entry point. Webpack exposes this module as
 * `./PluginConfigurationPanel`; the Signal K admin UI imports its default
 * export to render the panel.
 */

export { default } from './PluginConfigurationPanel.js'
