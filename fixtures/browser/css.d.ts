// The fixture imports the shared token sheet for its side effect only, to dress the host page during
// the screenshot run. Plain stylesheets carry no type declarations, unlike the panel's CSS modules.
declare module '*.css' {}
