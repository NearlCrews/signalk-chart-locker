# Milestone 3 handoff and continuation

The single starting point for resuming this work in a new session. Date 2026-06-27. The work
is on branch `main`, with no git remote configured.

## Where things stand

- Milestones 1, 1.5, and 2: complete (the Node plugin and lifecycle, the offline-GeoPackage
  storage proof, and the Rust engine port with its parity corpus).
- Milestone 3A (runtime `LocalProvider`): complete and merged. The container image builds and
  runs, and the router routes over a configured store.
- Milestone 3B (offline prep pipeline, `container/prep/`): the ENC core is implemented and
  verified against real NOAA cells. The DEPARE, DRGARE, and LNDARE paths and admin-0
  boundaries all work, and multi-cell overlapping-band precedence is verified (a harbour cell
  and a coastal cell over the same waters: the finer band covers the interior, the coarser
  fills gaps). Remaining: OSM water ingestion at real (multi-GB) scale, and more regions.
- Milestone 3C (data parity, `container/prep/data_parity.py`): the classification parity
  passed against the live NOAA ENC Direct service (San Francisco Bay: every point covered by
  both sources agreed on the `inEncDeep` and drying classifications). The Node lifecycle and
  fallback slice is done (`test/plugin-integration.test.ts`), and the one-way leg-level safety
  invariant passed on SF Bay (`data_parity.py --legs`: of 8 legs, all 5 the online ENC flagged
  unsafe were also flagged unsafe locally, with 0 silent safe flips). Remaining: OSM water at
  scale, and more regions.
- Milestone 4 (crows-nest cutover): not started.

## Repo map

- `src/`, `test/`: the Node plugin. Lifecycle, the `signalk-container` consumer, and the
  in-process bridge `globalThis.__signalk_binnacle_routeOnWater`.
- `container/engine/`: the Rust engine (the channel-router port) and the parity corpus. Pure
  geometry, no I/O dependencies.
- `container/localprovider/`: reads a region GeoPackage with `rusqlite` plus a pure-Rust WKB
  decoder and implements the engine `Provider` trait. The `testutil` feature exposes a
  `StoreBuilder` for tests and is excluded from release builds.
- `container/router/`: the axum service (`/health`, `/regions`, `POST /route-on-water`). It
  selects `LocalProvider` when `BINNACLE_REGION_STORE` names a store, `UnavailableProvider`
  (declines `fetch-failed`) on an open error, and `EmptyProvider` (declines `no-coverage`)
  when no store is set.
- `container/storage-spike/`: the Milestone 1.5 proof, kept as a historical record.
- `container/prep/`: the GDAL prep container. `prep_region.py` (build a region store),
  `data_parity.py` (compare a store to the live NOAA ENC), `Dockerfile`, `README.md`.
- `docs/superpowers/specs/`, `plans/`, `reviews/`, `decisions/`, `roadmap/`: the design spec,
  the per-milestone plans, the review records, the resolved decisions, and the cross-plugin
  migration roadmap.

## Verified state (evidence)

- Test suites all green: engine 54, localprovider 15, router 6, and the Node plugin 33.
- `cargo clippy --all-targets -- -D warnings` clean across the crates (the localprovider
  uses `--features testutil`). Release builds and the distroless router image build clean.
- Real-data path: a real NOAA harbour cell (US5CA13M, San Francisco Bay) built through the
  prep tool into a store that the router read through `LocalProvider`, returning a real
  `ok: true` water-following route that avoids charted land, with `usedTileWater: false`. The
  parity harness then matched the local classifications to the live NOAA ENC Direct service.
  The 3C one-way leg safety invariant also passed on that store against the live ENC: of 8 legs,
  all 5 the online ENC flagged unsafe were also flagged unsafe locally, with 0 silent safe flips.
  Reproducing that `ok: true` route required the prep polygon-only fix below: this cell's point
  and line LNDARE features were being written into `enc_land_areas` as MultiPoint and
  MultiLineString, which the polygon-only reader rejects, failing the whole harbour band to
  `None` so the router declined `no-coverage` for every request. After the fix the route returns
  `ok: true` with 3 waypoints and `usedTileWater: false` between two interior deep points.
- Full real-data store validated: a complete US5CA13M store with Natural Earth ocean as
  `osm_water` and admin-0 as `boundaries`, both clipped to the ENC extent by the prep clip step,
  decodes every table through the Rust `LocalProvider`: `charted_areas` 273 depth and 39 land,
  `tile_water` 2 polygons, and `foreign_rings` returns the US polygon for a non-US home and empty
  for `USA`. Routing is `ok: true` on the ENC depth areas. This is the first exercise of the
  `osm_water` and `boundaries` read paths against real data; prior tests used synthetic
  polygon-only fixtures.

## How to resume: build and test

```
npm test && npm run typecheck && npm run lint && npm run build
cd container/engine && cargo test && cargo clippy --all-targets -- -D warnings
cd container/localprovider && cargo test --features testutil && cargo clippy --all-targets --features testutil -- -D warnings
cd container/router && cargo test && cargo clippy --all-targets -- -D warnings && cargo build --release --bin router
```

First Rust builds on a Pi are slow: allow a long timeout.

## How to resume: build a region store

```
podman build -t binnacle-prep container/prep
# download the region's ENC cells (public domain), then unzip:
#   https://www.charts.noaa.gov/ENCs/<CELL>.zip   (each zip has ENC_ROOT/<CELL>/<CELL>.000)
podman run --rm -v /path/to/data:/work binnacle-prep \
  --enc-dir /work/enc --out /work/region.gpkg \
  [--boundaries /work/admin0.geojson --country-field ADM0_A3] [--osm /work/osm_water.gpkg]
```

The usage band comes from the cell name's third character (1 overview through 6 berthing).

## How to resume: run the router on a store

```
BINNACLE_REGION_STORE=/path/region.gpkg ROUTER_PORT=8080 container/router/target/release/router &
curl -s -X POST http://127.0.0.1:8080/route-on-water -H 'content-type: application/json' \
  -d '{"from":{"latitude":37.80,"longitude":-122.42},"to":{"latitude":37.79,"longitude":-122.39},"draftMeters":2.0,"safetyMarginMeters":0.5,"standoffNm":0.02,"borderAware":false}'
```

Request fields: `from`, `to` (each `{latitude, longitude}`), `draftMeters`,
`safetyMarginMeters`, `standoffNm`, `borderAware`, and optional `homeCountryId`, `corridor`,
`bboxAnchors`, `maxSnapMeters`, `deadlineMs`. Response is
`{ok:true, waypoints:[{latitude,longitude}], usedTileWater, borderFallback}` or
`{ok:false, reason}` where reason is one of `no-coverage no-path deadline unsnappable land-leg
fetch-failed` (or `router-unavailable` from the Node bridge on a transport failure).

## How to resume: run the data-parity check

```
podman run --rm -v /path/to/data:/work -v "$(pwd)/container/prep":/prep --entrypoint python3 \
  binnacle-prep /prep/data_parity.py --store /work/region.gpkg --band harbour --grid 8
```

## Remaining work, ordered, with the plan to follow

1. Finish 3B: OSM water ingestion at real (multi-GB) scale (`--osm`), and run prep over more
   regions. The `--osm` ingestion code path is verified on a small real sample: 98 OSM water
   polygons ingested into `osm_water` with a 1:1 R-tree and SRS 4326. Two things remain: the
   at-scale run with the real osmdata water-polygons product, and using the RIGHT water source.
   A raw `natural=water` Overpass subset omits the coastline-derived bay, so with it loaded the
   router safely declines `no-path` rather than routing the bay. Confirm the osmdata
   water-polygons split product (which includes the ocean) is the source.
   Plan: `docs/superpowers/plans/2026-06-27-companion-milestone-3b-prep-pipeline.md`.
2. Finish 3C: done. The one-way leg safety invariant passed on SF Bay
   (`container/prep/data_parity.py --legs`) and the Node lifecycle and fallback slice is covered
   (`test/plugin-integration.test.ts`). Running parity over more regions folds into item 1.
   Plan: `docs/superpowers/plans/2026-06-27-companion-milestone-3c-data-parity.md`.
3. Milestone 4: wire crows-nest to call the container behind a feature flag with the existing
   in-process fallback, which pulls the leg-safety geometry across. Cross-repo work in
   `signalk-crows-nest`.
4. The cross-plugin roadmap, highest standalone value first: a boat-wide offline tile and
   chart cache and proxy. See `docs/superpowers/roadmap/2026-06-27-cross-plugin-migration-candidates.md`.

## Decisions in force

- ENC distribution is Option A, pipeline-only: ship the prep pipeline, the owner downloads
  ENC cells per region, and chart data is never bundled in the repo or any image. See
  `docs/superpowers/decisions/2026-06-27-enc-distribution-model.md`.
- The engine parity bar is 2-ULP per coordinate, with the decline reason and the flags exact.
- `country_id` is the ISO 3166-1 alpha-3 code (`ADM0_A3` from the admin-0 source). The
  Milestone 4 caller must pass the same scheme as `homeCountryId`.
- The prep image is pinned to `ghcr.io/osgeo/gdal:ubuntu-small-3.10.3`.

## Gotchas worth knowing before you continue

- The rolling and Alpine GDAL builds of the S-57 driver segfault (exit 139) on valid NOAA
  cells. Keep the prep image on the pinned stable tag above.
- The runtime image must stay free of GDAL, GEOS, PROJ, and SpatiaLite. GeoPackage reads use
  `rusqlite` with the `bundled` feature plus the pure-Rust WKB decoder only. GDAL lives in the
  prep image alone.
- `LocalProvider` honesty: a successful but empty query returns present-but-empty so the
  engine declines `no-coverage`, and a SQL or decode error returns `None` so the engine
  declines `fetch-failed`. An undecodable geometry must fail loud, never drop an obstacle.
- The dev shell here is zsh (an unquoted variable does not word-split), a foreground `sleep`
  is blocked, and backgrounding the router inside a single shell call can exit 144: verify on
  a fresh port, or run it as a managed background task.
- `osm_water` is the engine's tile-water mask: when present it gates navigability, so the
  source must be a water layer that includes the navigable ocean and bay (the osmdata
  water-polygons split product), not a raw `natural=water` Overpass query, which is inland
  water only. A wrong or partial `osm_water` makes the router decline `no-path` (the safe
  failure), so such a store is not routable even though it ingested cleanly. An empty
  `osm_water` (an ENC-only store) is fine: the engine routes off the ENC depth areas with
  `usedTileWater: false`. To get a green route, pick endpoints actually inside a deep depth
  area (use `ST_PointOnSurface`, not `ST_Centroid`, which can land in a hole on a multipolygon)
  and reasonably close, so the padded route bbox resolves a grid: an `ok: true` route over
  US5CA13M between two interior deep points returns 3 waypoints with `usedTileWater: false`.
- The routing store is polygon-only: the `LocalProvider` WKB reader decodes Polygon and
  MultiPolygon and fails the whole band to `None` on anything else, a fail-loud safety backstop
  that never silently drops an obstacle. S-57 LNDARE carries point and line features too, so
  prep filters the ENC ingest to area geometry (`OGR_GEOMETRY LIKE '%POLYGON'`). Without that
  filter a real cell's MultiPoint or MultiLineString land features make every request decline
  `no-coverage`. Locked by a localprovider regression test.
- Border-aware routing is a no-op with admin-0 LAND boundaries. The engine blocks foreign
  jurisdiction over WATER, but Natural Earth admin-0 polygons are land and do not cover the
  navigable water, so `homeCountryId` USA versus CAN produced the identical route over the
  all-US bay. The `boundaries` table decodes and the home filter works (`foreign_rings` returns
  the US polygon for a non-US home), but the feature needs a maritime jurisdiction source
  (territorial sea or EEZ), not admin-0 land, to actually block foreign waters. Border-aware is
  part of the trust boundary, so resolve the boundaries data source before relying on it in M4.
- Prep now clips OSM water and boundaries to the ENC extent plus a 0.1 deg margin
  (`enc_region_bbox` plus `ogr2ogr -clipdst`), so a regional or global source does not write the
  whole world into a region store. A global Natural Earth ocean clipped to one harbour cell
  becomes one `osm_water` row.

## Boat-only tests (cannot be run without a live signalk-server)

1. The plugin loads under a real signalk-server and activates cleanly.
2. `signalk-container` launches the router container (`ensureRunning` succeeds and
   `resolveContainerAddress` resolves a reachable address).
3. With a real region store on the NVMe and `BINNACLE_REGION_STORE` set, the container returns
   a real route on the boat.
4. The crows-nest in-process bridge reaches the container, and falls back to in-process
   routing when the container is down.
